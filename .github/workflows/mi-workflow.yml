name: Pro-Wyscout-Femenino-Engine

on:
  schedule:
    - cron: '*/20 * * * *' # Cada 20 minutos
  workflow_dispatch: # Permite ejecución manual

jobs:
  scrape_and_analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          # Añadimos patchright a la lista de pip
          pip install scrapling supabase requests curl-cffi patchright
          # Instalamos el navegador específico de patchright
          python -m patchright install chromium
          python -m patchright install-deps chromium
          scrapling install
          
      - name: Run Orchestrator
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python main_orchestrator.py
